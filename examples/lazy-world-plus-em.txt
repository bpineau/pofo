# Lazy World + EM -- MSCI ACWI rebuilt from two cheaper blocks
# Idea: replicate the cap-weighted world (developed + emerging) with two
#   building blocks instead of one ACWI fund (World 88%, EM 12%): the same
#   broad equity beta at a lower blended fee.
# Inspired by: the classic Bogleheads two-ETF world equity build.
# UCITS, accumulating, 100% equity.

#meta sim:on
88    IWDA       # iShares Core MSCI World (IWDA)
12    EIMI       # iShares Core MSCI EM IMI (EIMI)
