# Risk-parity core + trend overlay (optimizer-weighted)
# Idea: a broadly diversified set (equity, long & short bonds, gold,
#   commodities, managed futures) left to the RISK-PARITY optimizer to weight —
#   every sleeve contributes a similar share of risk. Run it and read the
#   computed weights in the report.
# Inspired by: risk parity (Qian; AQR; Bridgewater) + the modern trend overlay.
# Note: the written weights are ignored — '#meta optimize:risk-parity' computes them.
#meta optimize:risk-parity
#meta sim:on

20    IWDA            # Equity
20    TLT             # Long bonds
15    SHY             # Short bonds
15    GOLD            # Gold
15    ICOM       # Broad commodities (ICOM)
15    KMLM            # Managed futures
