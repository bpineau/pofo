# Trend Sleeve Lab -- one engine per leg, compared in one portfolio
# Idea: the trend allocation of a decumulation portfolio should be split
#   across DISTINCT engines, not across products wrapping the same one.
#   This lab equal-weights the four accessible engine families: CTA-index
#   replication (DBi), two direct systematic trend managers (Winton, AQR)
#   and a rules-based pure-trend index without equity futures (MLM/KMLM).
#   Read the per-asset stats in the report to see how different "trend"
#   can be; correlation between legs is the number that matters.
# Anti-patterns this lab exists to expose: DBMFE + MFA8 (same DBi engine,
#   MFA8 merely drops commodities, i.e. drops the inflation hedge); paying
#   1.33% + perf fee for a fourth flavor of the same beta.
# The common window is short (capped by the youngest fund); read it as a
#   correlation lab, not a backtest.
#meta rebalance:180
#meta sim:on

25    DBMFE           # Engine A: CTA replication (iMGP DBi EUR, LU2951555403)
25    IE00BG382Q20    # Engine B1: Winton Trend UCITS (direct systematic trend)
25    LU1103257975    # Engine B2: AQR Managed Futures UCITS (direct, EUR-hedged share exists: LU1662502183)
25    KMLM            # Engine B3: MLM index, trend without equity futures (US-only fund; UCITS route: none yet)
